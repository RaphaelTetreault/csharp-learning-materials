```csharp
using System;

class Program
{
    static void Main()
    {
        // Declare a variable to store score.
        // Assign it the value of 0.
        int score = 0;
        Console.WriteLine($"The score value is '{score}'");
        
        // Add 100 points to score
        score += 100;
        Console.WriteLine($"The score value is '{score}'");
        
        // Add 2000 points to score
        score += 2000;
        Console.WriteLine($"The score value is '{score}'");
        
        // Add 9900 points to score
        score += 9900;
        Console.WriteLine($"The score value is '{score}'");
    }
}

```